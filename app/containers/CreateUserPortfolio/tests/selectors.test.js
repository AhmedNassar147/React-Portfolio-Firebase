// import { fromJS } from 'immutable';
// import { selectCreateUserPortfolioDomain } from '../selectors';

describe('selectCreateUserPortfolioDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
